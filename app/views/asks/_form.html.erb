<%= form_for(@ask) do |f| %>
  <% if @ask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ask.errors.count, "error") %> prohibited this ask from being saved:</h2>

      <ul>
      <% @ask.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_area :question %><br>
    <%= label_tag "Place of Birth" %><br>
    <%= text_area_tag 'Place of Birth' %>
  </div>
  <div class="actions">
    <%= f.submit("Click Here To Get Your Iching Reading") %>
  </div>
<% end %>
