<%= form_for(@ask, :format => :js, remote: true) do |f| %>
  <% if @ask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ask.errors.count, "error") %> prohibited this ask from being saved:</h2>

      <ul>
      <% @ask.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Please write your question in the box below." %><br>
    <%= f.text_area(:question, :cols => 50, :rows => 10) %><br>
  </div>
  <div class="actions">
   <div id="wrapper"> <%= f.submit("Click Here To Get Your Iching Reading") %></div>
    <style>#wrapper input {font-size:20px; background: #000000; color: #b29600}</style><br><br><br><br><br><br><br><br>
  </div>
<% end %>
